// 비교 연산자
let x = "10";
let y = 10;

x == y; // 값만 똑같으면 true
x === y; // 등호가 3개여야만 type까지 비교를 한다. 항상 등호 3개를 쓰기를 권고
x > y;
x >= y;
x < y;
x <= y;
x != y; // 값이 똑같으니 false를 출력할 것이다. x와 y는 똑같지 않다. false
// 왜? 값이 똑같으니까
x !== y; // 값과 타입까지 다른가?
